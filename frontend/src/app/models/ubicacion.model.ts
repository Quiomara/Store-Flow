/**
 * Interfaz que representa una ubicación dentro del sistema.
 */
export interface Ubicacion {
  ubi_ele_id: number; // Identificador único de la ubicación del elemento
  ubi_nombre: string; // Nombre de la ubicación
}
