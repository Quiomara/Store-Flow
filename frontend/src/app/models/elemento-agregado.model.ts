export interface ElementoAgregado {
  ele_id: number;                     // Identificador único del elemento.
  ele_nombre: string;                 // Nombre del elemento.
  pre_ele_cantidad_prestado: number;  // Cantidad del elemento que ha sido prestada.
  ele_cantidad_actual: number;        // Cantidad actual disponible del elemento.
}

  